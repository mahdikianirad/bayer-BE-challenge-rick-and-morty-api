{
  "name": "rick_and_morty_api",
  "version": "1.0.0",
  "description": "Small graphql api to fetch Rick and Morty Characters",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc && nodemon -r dotenv/config ./ts-built/index.js",
    "prebuild": "rm -rf ./ts-built && tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc"
  },
  "author": "Mahdi Kianirad",
  "dependencies": {
    "apollo-server-core": "^3.10.0",
    "apollo-server-express": "^3.10.0",
    "express": "^4.18.1",
    "graphql": "^16.5.0"
  },
  "devDependencies": {
    "typescript": "^4.7.4",
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.21",
    "dotenv": "^16.0.0",
    "nodemon": "^2.0.15",
    "tslint": "^6.1.3"
  }
}
